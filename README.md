# microscopic_segmentation_and_determination_tasks
After uploading the image, the system can automatically display the segmented image and analysis results, including the number of particle pixels, the number of trajectory pixels and the particle content. This graphical user interface (GUI) enables users to directly invoke the models and weight files developed in this research
